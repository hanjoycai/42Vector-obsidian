---
base: "[[机器学习-神经网络.base]]"
属于名称: 多层感知器(MLPs)
描述: 由多个相互连接的神经元层组成的神经网络。MLP可以学习输入和输出之间的复杂关系。
AIGC领域:
  - 神经网络
---
### 多层感知器的概念与基本组成

多层感知器（Multilayer Perceptron，简称MLP），是一种基于前馈神经网络（Feedforward Neural Network）的深度学习模型，由多个神经元层组成，其中每个神经元层与前一层全连接。多层感知机可以用于解决分类、回归和聚类等各种机器学习问题。
多层感知机的每个神经元层由许多神经元组成，其中输入层接收输入特征，输出层给出最终的预测结果，中间的隐藏层用于提取特征和进行非线性变换。每个神经元接收前一层的输出，进行加权和和激活函数运算，得到当前层的输出。通过不断迭代训练，多层感知机可以自动学习到输入特征之间的复杂关系，并对新的数据进行预测。

多层感知机（MLP，Multilayer Perceptron）也叫人工神经网络（ANN，Artificial Neural Network），除了输入输出层，它中间可以有多个隐层，最简单的MLP只含一个隐层，即三层的结构，如下图：

![[Untitled 198.png]]

**MLP 的层数**

1. **输入层**：该层接收输入数据并将其传递给后续层。
2. **隐藏层**：这是输入层和输出层之间的中间层。隐藏层执行复杂的处理，使网络能够学习复杂的数据表示。
3. **输出层**：该层产生网络的最终输出。

输入层—>隐藏层—>输出层

神经元：包含一个带有权重和偏置的线性变换，以及一个激活函数（通常，输入层不使用激活函数，隐藏层和输出层使用激活函数）用来引入非线性，使得神经网络可以任意逼近任何非线性函数，这样神经网络就可以利用到更多的非线性模型中

隐藏层神经元：假设输入层用向量X表示，则隐藏层的输出就是f(w1*X+b1)，函数f可以是sigmoid函数或者tanh函数，w1是权重(连接系数)，b1是偏置

输出层的输出：softmax(w2*X1+b2)，X1是隐藏层的输出

MLP训练过程：数据从输入层经过多个隐藏层的非线性变换，最后到达输出层进行分类或回归操作。通常使用反向传播算法，该算法通过计算损失函数对网络参数的梯度，并根据梯度更新参数，以最小化损失函数。常见的优化算法：随机梯度下降(SGD)、动量法、Adam等

### **为什么选择多层感知器（MLP）？**

单层感知器的局限性，尤其是无法解决像 XOR 问题这样的非线性可分离问题，导致了多层感知器（MLP）的发展。如下图所示，通过引入一个具有一个隐藏层的双层 MLP，可以轻松解决 XOR 问题。额外的隐藏层使网络能够创建中间表征，从而捕捉数据中的非线性关系。

![[image 832.png]]

根据通用逼近定理，只要有足够的容量（即足够多的神经元和适当的激活函数），包含有限数量神经元的单隐层 MLP 可以逼近任何连续函数。该定理强调了 MLP 的强大功能和灵活性。MLP 的突破极大地扩展了神经网络的能力，使其能够解决更广泛的问题。


### **多层感知机与卷积神经有什么区别？**

| 特征 | MLP | CNN |
| --- | --- | --- |
| 结构 | 由多个全连接层组成 | 由卷积层、池化层和全连接层组成 |
| 参数共享 | 每个神经元有自己的参数，参数数量较大 | 使用卷积核对输入进行卷积操作，通过参数共享来减少参数数量 |
| 特征提取能力 | 对图像等高维数据的特征提取能力相对有限 | 可以有效地保留输入数据的空间结构信息，具有出色的特征提取能力 |
| 应用领域 | 传统的机器学习任务以及一些简单的模式识别问题 | 计算机视觉领域广泛应用，如图像分类、目标检测、图像分割等任务 |

### **MLP 应用示例**

**回归示例：波士顿住房数据集**

在这个例子中，我们使用波士顿住房数据集来训练一个预测房价的神经网络模型，这是一个典型的回归问题。该数据集包括房屋的各种属性，如犯罪率、土地分区和房间数量。我们的目标是开发一个模型，能够根据这些输入特征准确预测房价的连续值。该应用展示了神经网络在处理具有多个输入变量的回归任务时的有效性。

![[image 833.png]]

**分类示例：虹膜数据集**

在本示例中，我们利用虹膜数据集实施了一个 3 层前馈神经网络来对虹膜花进行分类。鸢尾花数据集包括来自三个品种的 150 个鸢尾花样本：Versicolor、Setosa 和 Virginica。每个物种有 50 个样本，每个样本有四个特征：萼片长度、萼片宽度、花瓣长度和花瓣宽度。值得注意的是，其中一个物种与其他两个物种之间可以线性分离，而其余两个物种之间则不能线性分离。这项分类任务展示了多层感知器处理线性和非线性可分离数据的能力。

![[image 834.png]]