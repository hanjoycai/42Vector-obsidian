---
base: "[[机器学习-神经网络.base]]"
属于名称: MSE
描述: |-
  均方误差 (MSE） 有点像是一部电影的评分。如果你在影评平台上看到一部电影的评分接近满分，那么你会不会觉得这部电影很值得一看？
  如果我们的Al模型的MSE值很小，那就说明它的预测结果非常接近真实值，就像是一部接近满分的电影，大概率也是很精彩的。
  MSE越小，就说明我们的A模型越“出色”，它的预测结果越能真实反映出现实的情况。
AIGC领域:
  - 数据指标
---
神经网络中的损失函数是一种衡量模型预测结果与实际结果之间差距的方法。

在训练神经网络时，我们的目标是最小化损失函数以使模型更好地拟合数据，从而实现更准确的预期结果。

此处重点说一下“均方误差 (MSE)” 这个损失函数。

均方误差是预测值与真实值之间差值的平方和的平均值。

具体来说，如果我们有n个预测值和对应的实际值，MSE的计算公式就是：

其中，y_i表示实际值，y_表示预测值，n表示样本数量，z表示求和。

![[Untitled 316.png]]

在均方误差(MSE）的结果中，MSE 越小，模型拟合效果越好，说明预测模型的准确性越高。

相反，如果MSE的值较大，那么预测模型的淮确性就相对较低。

因此，在实际应用中，我们通常希望 MSE的值尽可能小，以获得更好的预测效果。

我们在训练Al神经网络时，如果首次 MSE的值为10，通过多次调整后，最后一次MSE的值为0.1， 

MSE从10降到了1，则说明

调整后的预测模型变得更淮确了。

![[Untitled 317.png]]

然而，不能仅从MSE的值来决定一个模型的好坏。

在不同的应用场景中，对于模型的预测精度要求是不同的。

例如，在某些对预测精度要求极高的场景中，即使MSE的值只有0.01，我们也可能认为这个模型的效

果不好， 而在一些对预测精度要求较低的场景中，即使MSE的值达到了0.1，我们也可能认为这个模

型的效果已经足够好。
