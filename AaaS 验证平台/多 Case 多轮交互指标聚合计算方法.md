

> **适用场景**：Agent 评测中，每个 Case 包含多轮模型交互，每个 Case 重复执行多次（如 10 次），最终需要汇总为报告级指标。 **核心挑战**：三层嵌套结构的数据如何正确聚合——不同指标的聚合方式完全不同，用错方式会严重失真。

---

## 一、数据结构

### 单任务无并发：完整的评测数据集的计算逻辑

```
全部评测集（20 个 Case）
 └── Case-1（SWE-001）
      ├── 第 1 次执行（Run-1）
      │    ├── R1: Planning      → input 2K,  output 500,  TTFT 135ms ...
      │    ├── R2: Read files    → input 4.2K, output 200,  TTFT 215ms ...
      │    ├── R3: Grep          → input 5.5K, output 100,  TTFT 285ms ...
      │    ├── R4: 分析代码       → input 8K,   output 1200, TTFT 420ms ...
      │    ├── R5: 生成修复       → input 12K,  output 2000, TTFT 650ms ...
      │    ├── R6: Write file    → input 14.5K, output 150,  TTFT 740ms ...
      │    ├── R7: 运行测试       → input 16.8K, output 800,  TTFT 870ms ...
      │    └── R8: 输出总结       → input 18.5K, output 300,  TTFT 980ms ...
      ├── 第 2 次执行（Run-2）
      │    └── R1 ~ R9（可能轮数不同）
      ├── ...
      └── 第 10 次执行（Run-10）
           └── R1 ~ R7（可能轮数不同）
 └── Case-2（SWE-015）
      └── Run-1 ~ Run-10
 └── ...
 └── Case-20（ICB-050）
      └── Run-1 ~ Run-10
```

聚合过程分三步走：

```
轮内原始数据 → 跨轮聚合到单次 Run → 跨 Run 聚合到单个 Case → 跨 Case 聚合到报告
```


### 单任务无并发：完整的评测数据集的计算逻辑


```
评测集（20 Case）
 └── 并发梯度（1 / 10 / 50 / 100 Agent）     ← 并发
      └── Case（20个，同时运行）
           └── Run（重复次数，视并发规模调整）
                └── Round（每个 Agent 的多轮交互）
```

**N 个 Agent 同时抢 GPU 资源各自跑各自的 Case**。

---


## 二、第一步：轮内计算——每一轮的原始指标

这是最底层的"原子数据"，每一轮直接可测量，不需要聚合：

|指标|直接测量方式|示例（R5 轮）|
|---|---|---|
|**TTFT**|`T(first_token) − T(request_sent)`|650ms|
|**TPOT**|`(T(last_token) − T(first_token)) / (output_tokens − 1)`|15.2ms/tok|
|**ITL 序列**|`{t₂−t₁, t₃−t₂, ..., tₙ−tₙ₋₁}`|[12, 14, 80, 13, 15, ...]|
|**P99 ITL**|ITL 序列的第 99 百分位|18.5ms|
|**E2E 延迟**|`T(last_token) − T(request_sent)`|31.0s|
|**Prefill 时延**|`T(first_token) − T(infer_start)`|180ms|
|**Decode 时延**|`T(last_token) − T(first_token)`|420ms|
|**input_tokens**|引擎日志直接读取|12,000|
|**output_tokens**|引擎日志直接读取|2,000|
|**工具调用耗时**|`T(tool_end) − T(tool_start)`|280ms|
|**工具调用结果**|pass / fail|✅|

这一步没有任何争议，纯粹是打点采集。

---


## 三、第二步：跨轮聚合——8 轮 → 1 次 Run 的结果

**这是最关键、最容易犯错的一步。** 不同指标的聚合方式完全不同。

---

### 3.1 TTFT：不做聚合，分拆报告

**❌ 错误做法**：把 8 轮 TTFT 取算术平均。

**为什么错**：TTFT 跟 input_tokens 近似线性——R1（input 2K）的 TTFT 是 135ms，R8（input 18.5K）的 TTFT 是 980ms，平均出来 ~500ms 这个数字毫无意义，既不代表最好情况也不代表最差情况。

**✅ 正确做法**：

```
单次 Run 的 TTFT 输出：
  ├── 首轮 TTFT = R1 的 TTFT = 135ms       ← 用户首次感知
  ├── 末轮 TTFT = R8 的 TTFT = 980ms       ← 最坏情况
  └── 各轮 (input_tokens, TTFT) 散点数据    ← 用于画散点图
```

---

### 3.2 TPOT：按 output_tokens 加权平均

**❌ 错误做法**：8 轮 TPOT 取算术平均。

**为什么错**：R3（Grep，output 100 token，TPOT 10.8ms）和 R5（生成修复，output 2000 token，TPOT 15.2ms），如果算术平均，两轮权重相同。但用户在 R5 等了 30 秒，在 R3 只等了 1 秒——R5 对体验影响大 30 倍。

**✅ 正确做法**：

**公式**：

$$TPOT_{weighted} = \frac{\sum_{r=1}^{8} TPOT_r \times output_tokens_r}{\sum_{r=1}^{8} output_tokens_r}$$

**计算示例**：

```
  R1: 12.1 × 500  =  6,050
  R2: 11.5 × 200  =  2,300
  R3: 10.8 × 100  =  1,080
  R4: 13.6 × 1200 = 16,320
  R5: 15.2 × 2000 = 30,400
  R6: 12.8 × 150  =  1,920
  R7: 14.5 × 800  = 11,600
  R8: 13.2 × 300  =  3,960
  ─────────────────────────
  分子 = 73,630
  分母 = 500+200+100+1200+2000+150+800+300 = 5,250

  加权 TPOT = 73,630 / 5,250 = 14.02 ms/tok
```

**权重分析**：R5（output 最多）对最终结果的贡献权重是 30,400/73,630 = **41.3%**，而 R3（output 最少）的权重仅 1,080/73,630 = **1.5%**。

---

### 3.3 ITL：取各轮 P99 的最大值

**❌ 错误做法**：把 8 轮所有 token 的 ITL 混在一起算 P99。

**为什么错**：混合后，R1-R4 的大量低 ITL token 会"稀释"R5 轮中那个 80ms 的毛刺——用户明明感知到了卡顿，但数据上被掩盖了。

**✅ 正确做法**：

```
每轮单独算 P99 ITL：
  R1: P99 = 15.2ms
  R2: P99 = 13.8ms
  R3: P99 = 12.5ms
  R4: P99 = 16.8ms
  R5: P99 = 18.5ms  ← 最大
  R6: P99 = 14.2ms
  R7: P99 = 17.5ms
  R8: P99 = 15.8ms

max P99 ITL = max(15.2, 13.8, 12.5, 16.8, 18.5, 14.2, 17.5, 15.8)
            = 18.5 ms/tok
```

这个值代表的是"用户在整个任务过程中感知到的最严重卡顿"。

---

### 3.4 E2E 延迟：分层报告，不要简单求和

**❌ 错误做法**：8 轮 E2E 求和当作"任务总时长"。

**为什么错**：轮与轮之间还有 Agent 编排调度的间隙（几十到几百毫秒），还有工具调用的等待时间。简单求和只是"模型推理累计"，不是任务全程。

**✅ 正确做法**：

```
单次 Run 输出三个 E2E：

① AaaS-Latency（任务 E2E）
   = T(task_end) − T(task_start)
   = 132s
   含义：用户感知的完整任务时长

② MaaS-Latency（模型推理累计）
   = Σ(各轮 E2E) = 6.2+2.5+1.3+16.8+31.0+2.6+12.5+5.1 = 78s
   含义：纯模型推理总耗时

③ 非推理开销
   = AaaS-Latency − MaaS-Latency = 132 − 78 = 54s
   含义：工具调用 + Agent 编排 + 网络传输等

④ 推理时间占比
   = 78 / 132 = 59.1%
```

---

### 3.5 Input Tokens：双口径统计

```
① 累计调用量 = Σ(各轮 input_tokens)
   = 2K + 4.2K + 5.5K + 8K + 12K + 14.5K + 16.8K + 18.5K
   = 81,500

② 去重信息量 = 各轮新增内容之和（去除重复的历史上下文）
   ≈ 20,000

③ 膨胀率 = 81,500 / 20,000 = 4.1×
```

---

### 3.6 任务功能正确性：二元判定

```
通过条件（以 Coding 场景为例）：
  ├── Agent 输出了修复 patch                      ✓
  ├── patch 能成功 apply 到代码库                   ✓
  ├── 目标测试（原本 fail 的）现在 pass              ✓
  └── 回归测试（原本 pass 的）仍然 pass              ✓

四项全满足 → 本次 Run = PASS
任一不满足 → 本次 Run = FAIL
```

---

### 3.7 单次 Run 的完整输出汇总

|指标|值|聚合方式|
|---|---|---|
|功能正确性|PASS|二元判定|
|首轮 TTFT|135ms|直接取 R1|
|末轮 TTFT|980ms|直接取末轮|
|加权 TPOT|14.02 ms/tok|output_tokens 加权|
|max P99 ITL|18.5 ms/tok|各轮 P99 取 max|
|AaaS-Latency|132s|墙钟时间|
|MaaS-Latency|78s|各轮推理求和|
|推理时间占比|59.1%|MaaS / AaaS|
|累计 Input tokens|81,500|各轮 input 求和|
|去重 Input tokens|20,000|去重后|
|膨胀率|4.1×|累计 / 去重|
|Output tokens|5,250|各轮 output 求和|
|总轮数|8|计数|
|工具调用次数|4|计数|
|工具调用全部成功|✓|全部 pass 才算 ✓|

---

---

## 四、第三步：跨 Run 聚合——10 次重复 → 1 个 Case 的结论

同一个 Case 跑 10 次，得到 10 张上面的表。现在要汇总成 1 个 Case 级结论。

---

### 4.1 功能正确性：用"多数通过"标准

```
10 次执行结果: [PASS, PASS, PASS, FAIL, PASS, PASS, PASS, PASS, FAIL, PASS]
通过次数: 8/10

判定规则：
  ├── 8/10 ≥ 8  → Case 判定为"通过"
  ├── 同时记录: pass@1 = 80%（单次通过率）
  └── 可选记录: pass@3 = 看连续3次中是否有1次通过
```

**为什么用 8/10 而不是 10/10**：Agent 有一定的随机性（即使 temperature=0，工具调用的返回也可能有差异），10/10 要求过于严苛。8/10 表示"稳定可复现地通过"。

**为什么不用 5/10（简单多数）**：5/10 意味着有一半概率失败，在生产环境不可接受。8/10 是"高置信度通过"。

---

### 4.2 性能指标：取中位数（不是均值）

```
10 次 Run 的加权 TPOT:
  [13.8, 14.2, 13.5, 14.8, 13.9, 14.1, 13.7, 22.5, 14.0, 13.6]
                                              ↑
                                         异常高（可能遇到 GC）

算术平均: 14.81 ms/tok  ← 被异常值拉高
中位数:    13.95 ms/tok  ← 稳定反映典型情况
```

**所有性能指标都取 10 次的中位数**，包括：

- 首轮 TTFT
- 末轮 TTFT
- 加权 TPOT
- max P99 ITL
- AaaS-Latency
- MaaS-Latency

中位数比均值的好处：对偶发抖动（GC、冷启动、网络波动）具有鲁棒性，1-2 次异常不会污染结果。

---

### 4.3 稳定性指标：10 次的变异系数

除了中位数，还要看"10 次之间的波动有多大"：

```
变异系数 CV = std(10 次值) / mean(10 次值) × 100%

判定标准：
  CV < 5%   → 非常稳定（绿灯）
  5-15%     → 正常波动（黄灯）
  CV > 15%  → 不稳定，需排查（红灯）
```

---

### 4.4 资源消耗：取中位数或均值均可

```
10 次 Run 的总轮数: [8, 8, 8, 9, 8, 8, 8, 10, 8, 8]
中位数: 8 轮

10 次 Run 的累计 input tokens: [81500, 81500, 81500, 92000, 81500, ...]
中位数: 81,500 tokens
```

如果 10 次中有某次轮数异常多（比如 10 轮），说明 Agent 走了弯路，需要额外轮次纠正——这本身就是一个诊断信号。

---

### 4.5 单个 Case 的完整输出

|指标|值|聚合方式|
|---|---|---|
|**Case 通过判定**|PASS（8/10）|≥ 8 次通过|
|**pass@1**|80%|10 次中通过的比例|
|**首轮 TTFT**|135ms|10 次中位数|
|**末轮 TTFT**|980ms|10 次中位数|
|**加权 TPOT**|13.95 ms/tok|10 次中位数|
|**TPOT 稳定性 CV**|4.2%|10 次变异系数|
|**max P99 ITL**|18.5 ms/tok|10 次中位数|
|**AaaS-Latency**|132s|10 次中位数|
|**MaaS-Latency**|78s|10 次中位数|
|**中位轮数**|8|10 次中位数|
|**累计 Input tokens**|81,500|10 次中位数|
|**膨胀率**|4.1×|10 次中位数|

---

---

## 五、第四步：单任务视角——"并发下每个 Agent 的体验"

**聚合方式**：在某个并发梯度下，把所有 Agent 的所有 Case 的指标汇总。

```
C=10 并发梯度下:
  ├── Agent-1 跑了 Case SWE-001, SWE-015 → 各自有中位 TPOT
  ├── Agent-2 跑了 Case SWE-042, SWE-078 → 各自有中位 TPOT
  ├── ...
  └── Agent-10 跑了 Case ICB-045, ICB-050 → 各自有中位 TPOT

  汇总方式:
    ① 所有 Agent 的所有 Case 的中位 TPOT，取全局中位数
    ② 或按 output_tokens 加权
    ③ 同时取 P50 / P90 / P99 分布
```


**产出表格**（每个并发梯度一行）：

|并发数|中位 TTFT(首轮)|中位 TPOT(加权)|中位 AaaS-Latency|Agent 任务完成率|决策准确率|平均轮数|
|---|---|---|---|---|---|---|
|C=1|135ms|13.8 ms/tok|132s|98.5%|96.8%|8.2|
|C=10|185ms|16.2 ms/tok|165s|97.2%|95.5%|8.8|
|C=50|620ms|19.5 ms/tok|280s|95.0%|94.8%|9.2|
|C=100|1,250ms|22.5 ms/tok|420s|92.5%|94.2%|9.5|


|并发数|RPS|输出吞吐量|Goodput|成功率|平均排队|KV Cache 峰值|被抢占数|超时数|限流数|
|---|---|---|---|---|---|---|---|---|---|
|C=1|5.2|69.1 tok/s|68.5 tok/s|99.1%|15ms|65%|0|0|0|
|C=10|42.5|842 tok/s|838 tok/s|99.6%|65ms|88%|12|18|5|
|C=50|180|2,800 tok/s|2,650 tok/s|97.5%|185ms|92%|85|62|28|
|C=100|310|3,850 tok/s|3,520 tok/s|96.8%|420ms|96%|185|120|55|


---



## 六、完整的并发测试聚合路径图

```
┌──────────────────────────────────────────────────────────────────────────┐
│                     并发测试聚合路径全景                                    │
├──────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  【原始数据】每轮 TTFT / TPOT / ITL / tokens / 排队时间 / 工具结果         │
│       │                                                                  │
│       ▼ 第一步: 跨轮聚合（与单任务基线相同）                                │
│  ┌──────────────────────────────────────────┐                            │
│  │ TTFT → 分拆报首轮/末轮                     │                            │
│  │ TPOT → 按 output_tokens 加权               │                            │
│  │ ITL  → 各轮 P99 取 max                    │                            │
│  │ E2E  → 任务E2E / 推理累计 / 非推理          │                            │
│  └────────────────────┬─────────────────────┘                            │
│                       │                                                  │
│       ▼ 第二步: 跨 Run 聚合（与单任务基线相同，但重复次数可能更少）           │
│  ┌──────────────────────────────────────────┐                            │
│  │ 正确性 → 多数通过判定                       │                            │
│  │ 性能   → 取中位数                           │                            │
│  │ 稳定性 → CV（样本少时标注置信度）             │                            │
│  └────────────────────┬─────────────────────┘                            │
│                       │                                                  │
│       ▼ 第三步: 跨 Case 聚合（分两个视角）                                  │
│  ┌──────────────────────────────────────────┐                            │
│  │                                          │                            │
│  │  视角 A：单任务视角                        │                            │
│  │  ├── 所有 Agent 的 Case 指标取中位数/分层   │                            │
│  │  ├── 产出：并发 C 下的中位 TTFT/TPOT/E2E   │                            │
│  │  └── 产出：并发 C 下的任务完成率/决策准确率  │                            │
│  │                                          │                            │
│  │  视角 B：系统视角                          │                            │
│  │  ├── 固定时间窗口内的系统聚合指标           │                            │
│  │  ├── 产出：RPS / 吞吐量 / Goodput / 成功率 │                            │
│  │  └── 产出：排队时间 / KV Cache / 抢占 / 错误│                            │
│  │                                          │                            │
│  └────────────────────┬─────────────────────┘                            │
│                       │                                                  │
│       ▼ 第四步: 跨并发梯度聚合（并发专属）                                  │
│  ┌──────────────────────────────────────────┐                            │
│  │ 扩展效率 = 实际吞吐 / 理论线性值            │                            │
│  │ 劣化倍率 = Metric(C) / Metric(C=1)        │                            │
│  │ SLO 达标率 = 满足SLO的请求比例随C变化       │                            │
│  │ ROI 曲线 = 节点ROI随并发变化                │                            │
│  │ 最大可服务并发 = SLO达标率≥99%的最大C值      │                            │
│  └──────────────────────────────────────────┘                            │
│                                                                          │
└──────────────────────────────────────────────────────────────────────────┘
```

---

## 七、关键原则速查表（并发测试补充）

|原则|说明|
|---|---|
|✗ 不要把不同并发梯度的数据混在一起算均值|每个梯度单独聚合，梯度之间做趋势分析|
|✗ 不要用 C=1 的通过率代表并发下的表现|分梯度报告，明确标注并发条件|
|✗ 不要只看系统吞吐量忽略单任务延迟|吞吐量和延迟是跷跷板，必须同时报告|
|✗ 不要把并发 Agent 数等同于并发请求数|Agent 有工具等待期，实际并发请求数动态变化|
|✗ 不要忽略并发下的隐性成本膨胀|轮数增加 + 失败沉没 = 真实成本比标称高 15-25%|
|✓ 固定测试时间窗口（非固定请求数）|保证不同芯片在相同时间压力下对比|
|✓ 分两个视角同时报告|视角 A（单任务体验）+ 视角 B（系统承载）|
|✓ 计算扩展效率和劣化倍率|描述"随并发增加，系统如何退化"|
|✓ 找到最大可服务并发数|SLO 达标率 ≥ 99% 的最大并发值|
|✓ 找到最佳运营并发点|ROI 曲线峰值对应的并发数|
|✓ 在 CSV 中新增并发标识字段|concurrency_level / agent_id，确保可追溯|